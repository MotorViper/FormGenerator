<Window x:Class="FormGenerator.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:FormGenerator"
        mc:Ignorable="d"
        Title="MainWindow" Height="29.7cm" Width="21cm">
    <Window.DataContext>
        <local:Data />
    </Window.DataContext>
    <ScrollViewer x:Name="mc_window" >
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <TextBox Grid.Column="0" Grid.Row="0"/>
            <Border Grid.Column="0" Grid.Row="1" Height="2" Background="Black"/>
            <Label Content="Label" Grid.Column="0" Grid.Row="2"  VerticalAlignment="Top" Margin="0,-5,0,0" FontSize="8" BorderThickness="1"/>
        </Grid>
    </ScrollViewer>
</Window>